# EasyCode Example Problem Bank

This tiny bank keeps a clean checkout runnable without bundling a full private
corpus. Put your real bank outside the application repository and point
`EASYCODE_PROBLEM_BANK_ROOT` at that directory. The directory must contain a
`Code/` folder.
